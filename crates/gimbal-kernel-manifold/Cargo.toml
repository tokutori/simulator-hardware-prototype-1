[package]
name = "gimbal-kernel-manifold"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
gimbal-core = { path = "../gimbal-core" }
manifold-rust = { version = "0.13.1", features = ["parallel"] }
rayon = "1.12.0"
thiserror.workspace = true
